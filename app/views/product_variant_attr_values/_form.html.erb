<%= form_with(model: product_variant_attr_value) do |form| %>
  <% if product_variant_attr_value.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_variant_attr_value.errors.count, "error") %> prohibited this product_variant_attr_value from being saved:</h2>

      <ul>
        <% product_variant_attr_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
