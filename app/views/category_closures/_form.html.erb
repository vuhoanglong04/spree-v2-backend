<%= form_with(model: category_closure) do |form| %>
  <% if category_closure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_closure.errors.count, "error") %> prohibited this category_closure from being saved:</h2>

      <ul>
        <% category_closure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
