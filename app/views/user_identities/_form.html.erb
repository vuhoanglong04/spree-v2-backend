<%= form_with(model: user_identity) do |form| %>
  <% if user_identity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_identity.errors.count, "error") %> prohibited this user_identity from being saved:</h2>

      <ul>
        <% user_identity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
