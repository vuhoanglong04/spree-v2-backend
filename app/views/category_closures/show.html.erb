<p style="color: green"><%= notice %></p>

<%= render @category_closure %>

<div>
  <%= link_to "Edit this category closure", edit_category_closure_path(@category_closure) %> |
  <%= link_to "Back to category closures", category_closures_path %>

  <%= button_to "Destroy this category closure", @category_closure, method: :delete %>
</div>
